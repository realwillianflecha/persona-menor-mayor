// Funcion sumar JavaScript
// 1. Declarando la funcion
function sumar(a, b){
   let resultadoSuma = a + b
   return resultadoSuma 
}

// Llamamos o invocamos la funcion
let argA = 5, argB = 7
let resultado = sumar(argA, argB)
console.log(`Resultado suma: ${resultado}`)